#!/bin/bash

if [ -f "main.c" ]; then
    echo "Let's not overwrite your current main.c ;)"
    exit 1
fi

cat > main.c <<EOF
/*
**
**
*/

#include <stdio.h>

int
main()
{

  return (0);
}
EOF

exit 0
